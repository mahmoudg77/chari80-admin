@model IEnumerable<Chair80CP.Models.sec_users_roles>

@{
    ViewBag.Title = "Index";
}


<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>
                    @ViewBag.Title
                </h2>
                <u class="header-dropdown m-r--5">
                    <li>
                        @Html.ActionLink("Create New", "Create","UserRoles", new { }, new { @class = "btn btn-primary btn-md addnew " })
                    </li>
                </u>

            </div>
            <div class="body">
                <div class="table-responsive">
    <div class="table-responsive">
        <table class="table table-striped table-hover js-basic-example dataTable">
            <thead>
                <tr>
        <th>
            @Html.DisplayNameFor(model => model.sec_roles.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sec_users.pwd)
        </th>
        <th></th>
    </tr>
	</thead>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sec_roles.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sec_users.pwd)
        </td>
        
		<td>
            @Html.ActionLink("edit", "Edit", "UserRoles", new { id = item.id }, new { @class = "btn text-success material-icons edit" })
            @Html.ActionLink("remove_red_eye", "Details", "UserRoles", new { id = item.id }, new { @class = "btn material-icons details" })
            @Html.ActionLink("delete", "Delete", "UserRoles", new { id = item.id}, new { @class = "btn text-danger material-icons delete" })
        </td>
			
    </tr>
}

</table>
</div>

                </div>            
            </div>
</div>
</div>
