@model Chair80CP.Models.tbl_cities

@using Newtonsoft.Json;

@{
    ViewBag.Title = "Edit/Country";
}



<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>
                    @ViewBag.Title
                </h2>
                <ul class="header-dropdown m-r--5">
                    <li>

                    </li>
                </ul>
            </div>
            <div class="body">
                <h2 class="card-inside-title">@ViewBag.Title</h2>
                <div class="row clearfix">
                    <div class="col-sm-12">
                        @using (Html.BeginForm("Edit", "city", FormMethod.Post, new { @class = "ajaxform general-validate" }))
                        {
                            @Html.AntiForgeryToken()



                            foreach (var item in Chair80CP.Libs.General.getLangData(Model.name))
                            {
                                try
                                {

                                    <div class="form-group">
                                        <div class="form-line">
                                            <input type="text" class="form-control" name="name[@item.Key]" value="@item.Value" placeholder="Name (@item.Key)" required />
                                        </div>
                                    </div>

                                }
                                catch (Exception)
                                {

                                    <div class="form-group">
                                        <div class="form-line">
                                            <input type="text" class="form-control" name="name[@item.Key]" value="" placeholder="Name (@item.Key)" required />
                                        </div>
                                    </div>

                                }
                            }

                            <div class="form-group">
                                @Html.DropDownList("country_id", null, htmlAttributes: new { @class = "form-control show-tick",required="" })
                            </div>


                            @Html.HiddenFor(model => model.id)

                            <div class="form-actions no-color">

                                <button type="submit" class="btn btn-success save">Save</button>
                                @Html.ActionLink("Back to List", "Index", "City", new { }, new { @class = "btn btn-primary back" })
                            </div>

                        }


                    </div>
                </div>



              
                   </div>
</div>
</div>
</div>
